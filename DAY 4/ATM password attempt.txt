ATM PIN Attempts

A user enters ATM PIN.
Allow maximum 3 attempts.
If correct PIN is entered within 3 attempts, allow access.
Otherwise, block the card.


FUNCTION main()
    correctpin=1234
    attempts=0
    access=atmaccess(correctpin,attempts)
    DISPLAY access
    END FUNCTION

FUNCTION atmaccess(correctpin,attempts)
    while attempt <= 3 THEN
    password=correctpin
    IF password == correctpin THEN
    Return "access granted"
    ELSE
    attempt=attempt+1
    END IF
    END WHILE
    Return "card blocked"
    END FUNCTION
        