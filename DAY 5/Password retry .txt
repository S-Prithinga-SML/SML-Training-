Password Retry System
User enters password.
Allow maximum 3 attempts.
If correct password entered → Login success
Else → Account locked


FUNCTION main()
    password=123
    attempts=0
    result=login(password,attempts)
    DISPLAY result
    END FUNCTION

FUNCTION login(password,attempts)
    userpassword=123
    while(attempt < 3)
    if password == userpassword then
    return "Login success"
    Else
    attempts=attempts+1
    End if
    End while
    return "Account locked"
    END FUNCTION